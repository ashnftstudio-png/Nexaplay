const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export function validateEmail(email: string) { if (!email.trim()) return "Email is required."; if (!emailPattern.test(email)) return "Enter a valid email address."; return undefined; }
export function validatePassword(password: string) { if (!password) return "Password is required."; if (password.length < 8) return "Use at least 8 characters."; if (!/[A-Z]/.test(password) || !/[a-z]/.test(password) || !/\d/.test(password)) return "Use upper, lower, and numeric characters."; return undefined; }
export function validateRequired(value: string, label: string) { return value.trim() ? undefined : `${label} is required.`; }
export function validateAuthForm(values: Record<string, string>, required: string[]) { const errors: Record<string, string> = {}; for (const key of required) { const err = validateRequired(values[key] ?? "", key.replace(/([A-Z])/g, " $1")); if (err) errors[key] = err; } if ("email" in values) { const err = validateEmail(values.email); if (err) errors.email = err; } if ("password" in values) { const err = validatePassword(values.password); if (err) errors.password = err; } if (values.confirmPassword !== undefined && values.password !== values.confirmPassword) errors.confirmPassword = "Passwords must match."; return errors; }
